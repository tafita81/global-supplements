name: Deploy Global Supplements

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3
    
    - name: 📦 Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        
    - name: 🔧 Install dependencies
      run: npm ci
        
    - name: 🏗️ Build application
      run: npm run build
      env:
        VITE_SUPABASE_URL: https://twglceexfetejawoumsr.supabase.co
        VITE_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InR3Z2xjZWV4ZmV0ZWphd291bXNyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTg5MjExOTAsImV4cCI6MjA3NDQ5NzE5MH0.kVKkE-dbIDi2-31-pCKBVzjjk5Hu-SV7SgmKzQVkaeY
        
    - name: 📋 Copy deployment files
      run: |
        cp .htaccess dist/.htaccess
        cp DEPLOY_HOSTINGER.md dist/ 2>/dev/null || true
        
    - name: 🗑️ Clean Hostinger server (force delete all)
      run: |
        sudo apt-get update -qq
        sudo apt-get install -y -qq lftp
        lftp -u "${{ secrets.HOSTINGER_FTP_USER }},${{ secrets.HOSTINGER_FTP_PASSWORD }}" "${{ secrets.HOSTINGER_FTP_HOST }}" <<EOF
        set ssl:verify-certificate no
        cd /public_html
        rm -rf *
        rm -rf .*
        bye
        EOF
        echo "✅ Servidor Hostinger limpo!"
        
    - name: 🚀 Deploy to Hostinger via FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.HOSTINGER_FTP_HOST }}
        username: ${{ secrets.HOSTINGER_FTP_USER }}
        password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
        local-dir: ./dist/
        server-dir: /public_html/
